import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks'
import Button from '~/components/atoms/Button.vue'

<Meta
  title="Components/Atoms/Button"
  component={Button}
  argTypes={{
    color: {
      control: { type: 'select', options: ['primary', 'warning', 'error'] },
    },
    label: {
      description: '`slot` Children',
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Button },
  template:
    '<Button :color="color" :outlined="outlined" :disabled="disabled" :block="block">{{label}}</Button>',
})

# Button

Buttons

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'テストボタン',
      color: 'primary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Outlined

<Canvas>
  <Story
    name="Outlined"
    args={{
      label: 'テストボタン',
      color: 'primary',
      outlined: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Outlined" />

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      label: 'テストボタン',
      color: 'primary',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Disabled" />

## Block

<Canvas>
  <Story
    name="Block"
    args={{
      label: 'テストボタン',
      color: 'primary',
      block: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Block" />
