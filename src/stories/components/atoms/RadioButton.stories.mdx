import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks'
import RadioButton from '~/components/atoms/RadioButton.vue'

<Meta
  title="Components/Atoms/RadioButton"
  component={RadioButton}
  argTypes={{
    color: {
      control: { type: 'select', options: ['primary', 'warning', 'error'] },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RadioButton },
  template:
    '<RadioButton :label="label" :color="color" :id="id" :name="name" />',
})

# RadioButton

RadioButtons

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      label: 'ラジオボタンのテキスト',
      color: 'primary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
